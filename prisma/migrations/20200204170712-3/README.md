# Migration `20200204170712-3`

This migration has been generated by new name at 2/4/2020, 5:07:12 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP TABLE "test"."User","test"."Post";

CREATE TABLE "test"."User" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    "email" text  NOT NULL DEFAULT '',
    "id" text  NOT NULL ,
    "username" text  NOT NULL DEFAULT '',
    PRIMARY KEY ("id")
) 

CREATE TABLE "test"."Post" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    "id" text  NOT NULL ,
    "published" boolean  NOT NULL DEFAULT false,
    "title" text  NOT NULL DEFAULT '',
    "user" text   ,
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "User.email" ON "test"."User"("email")

ALTER TABLE "test"."Post" ADD FOREIGN KEY ("user") REFERENCES "test"."User"("id") ON DELETE SET NULL
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200204131134-2..20200204170712-3
--- datamodel.dml
+++ datamodel.dml
@@ -1,23 +1,23 @@
 datasource pg {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql://postgres:SuperSecret@localhost:5432/userDB?schema=test"
 }
 generator ts  {
   provider = "prisma-client-js"
 }
 model User {
-  id              Int       @id @default(autoincrement())
+  id              String       @id @default(cuid())
   createdAt       DateTime  @default(now())
   username        String 
   email           String    @unique
   posts           Post[]               
 }
 model Post {
-  id          Int        @id @default(autoincrement())
+  id          String        @id @default(cuid())
   title       String 
   published   Boolean    @default(false)
   createdAt       DateTime  @default(now())
 }
```


